import DestinationPage from '../[slug]/DestinationPage'

export { generateMetadata } from '../[slug]/DestinationPage'

export default function RoumaniePage() {
  return <DestinationPage params={{ slug: 'roumanie' }} />
}
