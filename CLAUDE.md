# Heldonica — Claude Code

Le protocole commun à tous les agents est dans **`AGENTS.md`** : lis-le en
premier. Il n'y a qu'un texte, pour que Claude, Gemini et OpenCode suivent les
mêmes règles — ce fichier ne fait que te l'indiquer.

Ce qui est propre à Claude Code sur ce poste est dans la mémoire du projet
(`~/.claude/projects/…/memory/`), pas ici.
